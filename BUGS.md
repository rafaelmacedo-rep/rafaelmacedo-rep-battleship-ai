# Bug log

Every bug found while building the game, **oldest first** — the log reads as the history of
the project. Entries are numbered sequentially in that chronological order and grouped
under the area they belong to, so numbers are not contiguous inside a section.
Format: **Symptom → Root cause → Fix → How verified**.

## Summary

- **5 bugs logged**: 2 before PR #1 (self-review of the first draft), 1 in PR #3 (found
  while writing the unit tests), 2 in PR #4 (found by playing the game in the browser).
- **Found by my own analysis or testing: 5. Reported by the user: 0. Caught by a failing
  test: 0** — the AI line-merging bug (#3) was found by reasoning about the targeting code
  while writing `tests/ai.test.js`, and the test that pins it was written afterwards.
- **1 of 5 has a regression test** (#3). The other four are CSS-only or code-hygiene
  issues with no DOM/CSS test harness in the project; they were verified in the browser.
- Areas with no logged bugs: firing/turns and end-game/reset — both are covered by unit
  tests (`tests/board.test.js`, `tests/game.test.js`) that passed from the first run.

## Placement

### 4. Hovered cell never showed the placement preview colour

- **Symptom:** during placement, hovering a cell shaded the rest of the ship green (or red
  when invalid) but the hovered cell itself stayed the plain hover blue, so a Carrier
  preview looked 4 cells long instead of 5. Same for the invalid/red preview.
- **Root cause:** `.cell:enabled:hover` in `styles.css` has higher specificity (class plus
  two pseudo-classes) than `.cell.preview` / `.cell.preview-invalid`, so it won on the one
  cell that is both hovered and part of the preview — reordering the rules would not have
  helped.
- **Fix:** excluded preview cells from the hover rule:
  `.cell:enabled:not(.preview):not(.preview-invalid):hover`.
- **How verified:** in the browser, hovering A1 with a horizontal Carrier now paints all of
  A1-E1 green (computed background of A1 is the preview green, not the hover blue), and an
  invalid position paints all five cells red.

## Firing and turns

No bugs logged in this area. Illegal shots (off-board, repeated), turn hand-over and the
"a repeated shot does not cost you the turn" rule are enforced in `fireAt` / `playerFire`
and were covered by tests in PR #3 without any of them failing.

## AI opponent

### 2. Dead `inBounds` guard in the placeholder AI

- **Symptom:** none observable at runtime — the Stage 1 `randomShot` scanned the board and
  filtered candidate cells through `inBounds(row, col)`, a condition that is always true
  inside the `0..BOARD_SIZE-1` loops, and imported `inBounds` from `constants.js` only for
  that check.
- **Root cause:** the guard was copied from `fireAt`, where the coordinates come from the
  outside and really can be off-board. In `randomShot` the coordinates are generated by the
  loops, so the check could never fail and only obscured the intent of the filter.
- **Fix:** dropped the guard and the now-unused import, leaving
  `if (!alreadyFiredAt(board, row, col)) candidates.push({ row, col });`.
- **How verified:** found in a review of the first draft before PR #1 was opened, together
  with #1. After the change, the Node smoke script drove a full game to a winner and every
  AI shot was still a fresh, in-bounds cell. This is a correctness-of-code issue rather
  than a user-visible bug; it is logged because it was one of the two changes that review
  pass produced.

### 3. AI could chase a ship line that did not exist

- **Symptom:** when two ships sat in the same row with a gap between them (e.g. a
  Destroyer on row 5 columns 0-1 and a Cruiser on row 5 columns 5-7), the AI stopped
  probing around the hits it had and instead fired at the far ends of the whole row,
  wasting shots on empty water between and beyond the two ships.
- **Root cause:** `lineThrough` in `src/ai.js` grouped hits by *any* shared row or column,
  not by adjacency, so two hits on the same row were treated as one long ship. The target
  queue then only contained `min(col) - 1` and `max(col) + 1` of that fake run, discarding
  the real neighbours of each hit.
- **Fix:** `runThrough` walks outwards from a hit while the next cell is also a hit, so
  only an unbroken run counts as a line; `lineThrough` returns a line just for runs of
  length 2 or more, and isolated hits fall back to their four neighbours.
- **How verified:** added `ai.test.js` case "does not merge two ships that share a row
  into one line", which hits both ships once and asserts the queue still probes around
  the first hit and never contains the gap cell between them. It fails on the old code and
  passes on the fixed code; the 500-game simulation still finishes every game without a
  repeated shot, averaging ~54 shots.

## End-game and reset

No bugs logged in this area. Win/lose detection, the game refusing shots once it is over
and "Play again" rebuilding the state (including a fresh AI memory via `createBrain()`)
were covered by tests in PR #3 and by a browser pass, and never misbehaved.

## UI and CSS

### 1. Log panel had a broken CSS declaration

- **Symptom:** the `.log` block's `max-height` was ignored in the first draft, so a long
  game stretched the side panel instead of scrolling.
- **Root cause:** a stray newline split the declaration (`max-height: 260px` and `;` on
  separate lines), which the browser dropped along with the following `overflow-y` in some
  parses.
- **Fix:** joined the declaration back into `max-height: 260px;` in `styles.css`.
- **How verified:** caught while reviewing the stylesheet before the first commit; after
  the fix, a long game in the browser keeps the log scrolling inside a fixed-height panel
  and the board layout no longer shifts.

### 5. Fired cells and the enemy board looked washed out

- **Symptom:** every already-fired enemy cell — and the whole enemy grid while the AI was
  firing — rendered at 45% opacity, muting the hit red and miss pale so board state was
  hard to read.
- **Root cause:** grid cells are `<button>` elements and get disabled once fired (or while
  it is not the player's turn), so the generic `button:disabled { opacity: 0.45 }` rule for
  the control buttons applied to them too.
- **Fix:** `.cell:disabled { opacity: 1 }`, which outranks the element-level rule, so only
  real control buttons fade when disabled.
- **How verified:** in the browser, a fired enemy cell and the enemy grid during the AI's
  turn both compute `opacity: 1` and show full-strength hit/miss colours; the disabled
  "Start game" button still fades.

## Note on the pre-PR #1 review pass

The review of the first draft, run just before PR #1 was opened, produced exactly the two
changes logged above as #1 and #2, plus one observation that was deliberately left alone
(player-board cells stay clickable during play — see the first known limitation below). The
recovered history of that pass — the review notes, the two file edits and the diff between
the first draft and the commit that became PR #1 (`fb6f234`) — contains no further fixes, so
no additional early entries are claimed here rather than guessed at.

## Open / known limitations

Things known to be imperfect and deliberately out of scope. None of these are logged as
bugs above because none of them has been fixed.

- **Player-board cells are clickable no-ops during play.** They are `<button>` elements and
  are never disabled once the battle starts; clicking one calls `placePlayerShip`, which is
  guarded on `phase === 'placement'` and does nothing. Harmless, but the cursor and focus
  ring suggest an action that does not exist.
- **No automated coverage of the DOM or CSS.** `render.js` and `styles.css` have no tests,
  which is exactly why bugs #1, #4 and #5 were only caught by looking at the page. Both CSS
  fixes are held in place by nothing but the stylesheet itself.
- **The test suite is not wired into CI.** `node tests/run-node.mjs` (or the browser page)
  has to be run by hand, so a regression could reach `main` unnoticed.
- **`randomPlacement` retries until it fits.** `while (!placed)` re-rolls a random position
  for each ship; with the standard 10x10 board and five ships this always terminates
  quickly, but a denser fleet or smaller board could make it spin for a long time.
- **The AI's hunt phase assumes every ship is at least 2 cells long.** The parity lattice
  cannot miss a ship of size >= 2, but adding a size-1 ship to `FLEET` would silently break
  that guarantee. The AI also does not weight cells by which ship sizes are still afloat,
  which is the next real strength improvement.
- **No persistence and no cancel of the AI's turn.** Refreshing loses the game, and the
  600 ms `setTimeout` before the AI fires cannot be interrupted.
- **Log is capped at 30 entries** in `render.js`; older shots scroll out of existence rather
  than being kept in a longer history.
